# StockPortfolioMonitor 開發指引

## 實作順序與階段門檻

- V2 以 `IMPLEMENT_PLAN_WEB_DASHBOARD_V2.md` 為歷史範圍基準；V3 遷移以 `V3_MIGRATION_PLAN.md` 為目前階段基準。
- V2 Phase 0 至 Phase 9 已完成並必須維持相容。V3 M0 至 M2 已完成，下一個工作階段是 M3（Trend + Momentum V3 + Relative Strength）。
- 嚴格依 V3 M0 → M9 順序實作；未通過當期全部驗收條件前，不得開始下一階段。
- 不得提前加入計畫明列為後續階段的功能，即使相關實作看似方便。
- 每個階段完成時必須：使用真實官方資料、執行測試、記錄驗收與來源限制、提交 Git，並在已設定遠端時推送。
- 資料不足或官方來源不明時，明確記錄阻礙；不得以產生、樣本、插值或推測值補進正式流程。

## Token 與工具輸出控制

- 搜尋優先使用 `rg`，只讀取相關函式、行段或必要檔案；已完整讀過的計畫與大型文件不要重複整份輸出。
- 成功測試使用摘要模式，例如 `pytest -q`；只有失敗時才取得完整堆疊，修正後回到摘要。
- 大量資料驗證交由本機腳本執行，正常輸出只保留狀態、筆數、日期範圍與少量代表性抽查。
- 遠端逐日下載只在每 25 日回報一次進度；每筆請求的開始／成功事件使用 DEBUG，不送入一般對話輸出。失敗日期與原因仍以 INFO／ERROR 顯示。
- 瀏覽器驗證只檢查必要控制項、圖例、版面與一筆代表性 hover，不讀取完整 Plotly DOM 或全部資料點。
- 優先重用 `data/cache` 內已驗證且具 SHA256 的官方回應，避免重複下載及重複日誌。
- 對使用者的進度更新保持簡短：目前完成度、已知問題、下一個驗收點。

## 資料正確性

- 正式分析只能讀取官方取得、正規化、驗證並持久化的資料。
- 保留來源、來源類型、官方標記、擷取時間、交易日期與單位。
- `FAIL` 必須阻擋持久化或正常分析；警告不得靜默忽略。
- 融資融券維持官方「交易單位」；法人維持「股」。不可擅自互換單位。
- 歷史分析禁止使用未來資料；Phase 3 起必須保存 pivot_date 與 confirmation_date。
